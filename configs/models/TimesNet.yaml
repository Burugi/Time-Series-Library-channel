# TimesNet model-specific hyperparameters
model_params:
  d_model:
    type: categorical
    choices: [64, 128, 256, 512]

  d_ff:
    type: categorical
    choices: [128, 256, 512, 1024]

  e_layers:
    type: int
    min: 1
    max: 3

  top_k:
    type: int
    min: 1
    max: 5

  num_kernels:
    type: int
    min: 4
    max: 8

  embed:
    type: categorical
    choices: ['fixed', 'timeF']
